import styled from 'styled-components';

const Button = styled.button`
  font-size: 20px; border: none; padding: 10px 14px;
  background: rgba(0,0,0,.8); border-radius: 10px;
  color: white;font-weight:bold;
`;

export {Button}