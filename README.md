# A simple and fast accounting application
## installation
After downloading the project, run it in the project root directory
- `npm install` ,Install dependencies
- `npm run build`，package the source code